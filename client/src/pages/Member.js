import React from 'react';
import PropTypes from 'prop-types';

const Member = ({ }) => {

    React.useEffect(() => {

    }, []);

    return (
        <div>

        </div>
    )
}

Member.propTypes = {

}

export default Member;